// The marketing landing page lives in src/landing (Tailwind-scoped, self-contained).
// This module stays the route entry point so App.jsx keeps a single import for "/".
import LandingPage from '../landing/LandingPage';

export default LandingPage;
