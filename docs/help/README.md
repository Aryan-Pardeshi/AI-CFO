# App help docs

Owned by `feat/calculators-seed`. This directory backs the `get_app_help(topic)` agent
tool — curated content the model reads, not content it generates. One markdown file per
topic, filename = topic slug used by the tool.

See `.agents/agent-guide.md#tool-registry` for the tool contract.
