@AGENTS.md

## Gemini CLI specific notes

- Same rules as everyone else in this repo — see AGENTS.md and your section in
  `.agents/modules.md` (`# Module: <your-module>`).
- Tool-name mapping for this environment (if using GEMINI.md's own tool-equivalence
  conventions) follows whatever the local Gemini CLI setup already maps — no repo-specific
  overrides needed here.
